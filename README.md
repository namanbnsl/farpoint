# Farpoint

```bash
npx farpoint-cli
```

## Development

Farpoint uses Node.js 24 and the Vite+ toolchain.

```bash
pnpm install
pnpm check
pnpm build
```

Inspect the exact npm artifact before publishing:

```bash
pnpm pack:dry
```
